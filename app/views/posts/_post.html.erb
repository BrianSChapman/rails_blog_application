<div class= "container">
<div id="<%= dom_id post %>">
  <h4><%= post.title %></h4>
  <h6>posted by <%= link_to post.user.name, user_path(post.user) %><h6>
<h7><%= pluralize( post.views, "view") %></h7>
  <p>
    <strong>Body:</strong>
    <%= post.body %>
  </p>
</div>
</div>
